{
  "name": "neunoval-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install",
    "start": "node api/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "path": "^0.12.7"
  }
}